# Database Migrations

Versioned, forward-only migration files for the ZoreeTMS Supabase database.

## Rules
- All schema changes go through migration files — never ad hoc
- Files are numbered sequentially: `001_`, `002_`, etc.
- Each file includes: description, affected APIs/UI, backfill needs, rollback SQL, risks
- Run migrations via Supabase Dashboard > SQL Editor (copy-paste)
- Document what was run and when

## Migration Log

| # | File | Description | Run Date | Run By |
|---|------|-------------|----------|--------|
| 001 | `001_add_rate_id_to_shipments.sql` | Add rate_id column to shipments | 2026-04-05 | Tulasi |
| 002 | `002_create_documents_table.sql` | Create documents table | _pending_ | _pending_ |
| 003 | `003_create_warehouse_dock_config.sql` | Warehouse dock configuration table | _pending_ | _pending_ |
| 004 | `004_add_dock_issue_to_shipments.sql` | dock_issue column on shipments | _pending_ | _pending_ |
| 005 | `005_add_oms_sync_columns.sql` | REQ-01: sync_source, auto_synced_at, oms_order_ref on orders | 2026-04-16 | Sridhar (SQL Editor) |
| 006 | `006_create_change_history.sql` | REQ-02: change_history table | 2026-04-16 | Sridhar (SQL Editor) |
| 007 | `007_create_user_profiles.sql`  | REQ-08: multi-role user_profiles table | 2026-04-17 | Sridhar |
| 008 | `008_create_invoices_and_carrier_tolerance.sql` | REQ-06: invoices table + carrier tolerance columns | _pending — apply via run-migration.js or SQL Editor_ | _pending_ |
| 009 | `009_change_history_add_invoice.sql` | REQ-06: allow 'invoice' in change_history.entity_type | _pending — apply via run-migration.js or SQL Editor_ | _pending_ |
| 010 | `010_invoices_add_shipment_ids.sql` | REQ-07: shipment_ids[] on invoices for consolidated invoicing | _pending — apply via run-migration.js or SQL Editor_ | _pending_ |
| 018 | `018_req24_ship_from_to_name_columns.sql` | REQ-24: ship_from_name/ship_to_name on orders & shipments; origin_zip/dest_zip on shipments | 2026-04-20 | Sridhar |
| 019 | `019_req24_oms_orders_ship_fields.sql` | REQ-24 (OMS): ship_from_name/city/state + ship_to_name/city/state on oms_orders | 2026-04-20 | Sridhar |
| 020 | `020_req24_drop_origin_location_fk.sql` | REQ-24 (OMS): drop oms_orders_origin_location_fkey so origin_location can hold composed free-text addresses | _pending — apply via run-migration.js or SQL Editor_ | _pending_ |
| 021 | `021_rates_add_match_type_and_geo.sql` | Rates: match_type enum (city_to_city / zip_to_zip / country_to_country) + origin_zip/dest_zip + origin_country/dest_country; shipments: origin_country/dest_country. Backfill regex-extracts ZIPs from existing free-text origin/dest. | 2026-04-25 | Sridhar (SQL Editor) |
| 022 | `022_normalize_carrier_names_uppercase.sql` | Canonicalize carrier names to UPPERCASE on rates.carrier / carriers.name / shipments.carrier (data backfill + BEFORE-INSERT/UPDATE triggers). Fixes silent rate↔carrier match misses caused by mixed-case seed data. | 2026-04-25 | Sridhar (SQL Editor) |
| 023 | `023_req24_oms_push_tracking_columns.sql` | REQ-24 (OMS): add tms_order_pushed_at / tms_ship_status_pushed_at / tms_pod_pushed_at on oms_orders + partial indexes + backfill. Unblocks inline tender-accept auto-sync (supersedes mistargeted 010_oms_add_tms_push_tracking.sql, which was never applied). | 2026-04-21 | Sridhar |
| 024 | `024_rates_add_equipment.sql` | Add `equipment` column to rates (soft reference to equipment_types.name) + backfill LTL→'LTL' / TL→'Dry Van 53ft'. Unblocks the planner replacing hardcoded LTL_MAX/TL_MAX constants with equipment_types.max_weight. | 2026-04-22 | Claude (Supabase MCP) |
| 025 | `025_shipments_add_equipment.sql` | Add `equipment` column to shipments so the planner can snapshot the rate's trailer onto the shipment row at planning time. No backfill — legacy rows stay NULL. | 2026-04-22 | Claude (Supabase MCP) |
| 028 | `028_rename_partial_mode_to_parcel.sql` | Rename shipment mode value `Partial` → `Parcel` across `dock_loading_durations` (PK + label) and every operational table that carries `mode` (shipments, orders, oms_orders, documents, route_templates). Wrapped in a single transaction; tables guarded with `information_schema` checks. | _pending — apply via SQL Editor_ | _pending_ |
| 031 | `031_shipments_add_commodity.sql` | Snapshot the consolidated commodity onto `shipments` so the Shipment Details modal renders a real value on Copy Shipment (which intentionally drops `order_ids`) and on shipments with no currently-linked orders. Backfill aggregates `DISTINCT orders.commodity` per shipment using the same comma-join rule as `deriveCommodityFromOrders`. Bulk-plan and copy paths snapshot at write time. | 2026-05-03 | Sridhar (SQL Editor) |
| 032 | `032_shipments_add_line_items.sql` | Snapshot consolidated `order_lines` onto `shipments.line_items` (JSONB, default `[]`) so the Shipment Details Line Items table renders on Copy Shipment without duplicating ORD- rows. Backfill aggregates per shipment via `orders.shipment_id JOIN order_lines`, ordered deterministically. Modal prefers the live per-order fetch when orders are linked and falls back to the snapshot only when they are not. Bulk-plan and copy paths snapshot at write time. | 2026-05-04 | Claude (Supabase MCP) — fixes "Failed to copy: PGRST204 line_items"; 268/363 rows backfilled, schema cache reloaded |
| 033 | `033_create_change_history_clears.sql` | TMS bug #1: persistent Clear History. New `change_history_clears` table records per-(entity_type, entity_id) `cleared_at` markers; `changeHistory.getHistory()` filters audit rows older than the latest marker. Audit ledger itself is never deleted (REQ-02). Composite index on (entity_type, entity_id, cleared_at DESC) keeps the per-open lookup a single index seek. Supersedes the misplaced `supabase/migrations/20260503_change_history_clears.sql`, which was never applied. | 2026-05-04 | Claude (Supabase MCP) |
| 034 | `034_orders_add_ref_num.sql` | Add nullable `orders.ref_num` text column to back the user-editable Reference # field on the Order Detail Edit modal. The whole stack (`OrdersPage.jsx#saveOrderEdit`, `orderMutations.js#apiOrderToDbPatch`, `server.js#ORDER_HISTORY_FIELDS`, `historyService.js`) already spoke `ref_num`, but the column never existed — so PATCH /api/orders/:id was silently rejected by PostgREST and Reference # / PO Number / Service Level edits never landed in `change_history` (the failed PATCH took down the whole row update, taking the other two fields' diffs with it). Distinct from `oms_order_ref` (OMS-side traceability ID, not user-editable). Companion code fix in same change set: `api/server.js#dbToOrderApi` exposes `ref_num` + `po_number` (with `refNum` / `poNum` camelCase aliases) in the API response shape so the UI can re-render saved values after PATCH/refresh. Idempotent (`ALTER TABLE … IF NOT EXISTS`). | 2026-05-04 | Claude (Supabase MCP) |
| 035 | `035_backfill_shipment_cost_breakdown.sql` | Data-only backfill. Repairs `shipments.rate` + `shipments.fuel_surcharge` on rows produced by the "Schedule" / "Plan All" path while `bulkPlanOrders` was omitting the breakdown from its inline plan body (51 rows repaired). Reconstructs base = ROUND(total_cost / (1 + fsc%/100), 2) and fuel = total_cost − base from the linked rate's fsc string. Idempotent on `rate=0 AND fuel_surcharge=0 AND accessorials=0`. Skips rows where the rate has been deleted/renamed or carries an empty fsc. The code-side fix lives in the same change set (`frontend/src/services/ordersService.js#bulkPlanOrders` four plan-body builders now mirror `confirmPlan`/`buildPlan`). | 2026-05-04 | Claude (Supabase MCP) |
| 036 | `036_shipments_status_add_tender_accepted.sql` | QA #61: extend `chk_shipments_status_controlled` with `'Tender Accepted'` so accepting a tender on the carrier-portal flow can mirror the value onto the linked shipment row; previously the constraint blocked the write and the mobile flow kept shipment status at `'Tendered'`, producing the QA-reported desync ("Order shows Tender Accepted but Shipment doesn't"). Strictly additive whitelist expansion. Companion code: `api/routes/shipments.js#VALID`, `mobile/src/services/carrierPortalService.ts#saveTenderResponse`. | 2026-05-05 | Sridhar (SQL Editor) |

> **Note on the duplicate `010_` filenames:** two files share the `010_` prefix — `010_invoices_add_shipment_ids.sql` (REQ-07, logged above) and `010_oms_add_tms_push_tracking.sql` (never applied; its header wrongly claimed a separate OMS Supabase project). The latter is superseded by `023_…` and kept on disk for history only.
